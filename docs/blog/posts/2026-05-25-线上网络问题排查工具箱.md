---
title: 线上网络问题排查工具箱
date: 2022-03-26
tags: 网络, 排查工具, curl, tcpdump, arthas, wireshark, 运维
summary: 汇总curl/tcpdump/wireshark/arthas等网络排查工具的实战用法，附带三个真实排查案例
author: ai-helper
---

## 前言

线上网络问题是最让人头疼的一类故障。CPU 高了可以看火焰图，内存溢出了可以 dump 分析，但网络问题——"接口偶尔超时"、"连接突然断开"、"部分用户反馈打不开页面"——这些描述本身就模糊，排查起来经常像无头苍蝇。

这篇文章整理了我在过去两年中反复使用的网络排查工具和方法，附带三个真实案例。工具不在多，在于用得熟练。

## 工具一：curl — 最基础的排查利器

curl 不只是"发个请求"那么简单，它的 `-v`（verbose）和 `-w`（write-out）选项能提供大量诊断信息。

### 查看完整的请求/响应过程

```bash
curl -v -o /dev/null https://api.example.com/health 2>&1

# 关注这些关键信息：
# * TCP_NODELAY set           → TCP 连接开始
# * Connected to ...          → TCP 连接建立成功
# * ALPN, server accepted to use h2  → 协议协商（HTTP/2）
# * SSL connection using TLSv1.3     → TLS 握手信息
# < HTTP/2 200                → 响应状态码
# < content-type: ...         → 响应头
```

### 测量请求各阶段耗时

```bash
curl -o /dev/null -s -w "
DNS解析:       %{time_namelookup}s
TCP连接:       %{time_connect}s
TLS握手:       %{time_appconnect}s
首字节时间:     %{time_starttransfer}s
总耗时:        %{time_total}s
下载大小:      %{size_download} bytes
HTTP状态码:    %{http_code}
" https://api.example.com/health

# 典型输出：
# DNS解析:       0.012s
# TCP连接:       0.015s
# TLS握手:       0.089s
# 首字节时间:     0.156s
# 总耗时:        0.167s
```

如果 DNS 解析时间长，说明 DNS 有问题。如果 TCP 连接时间长，说明网络不通畅或目标端口有问题。如果 TLS 握手时间长，说明证书配置或 TLS 版本有优化空间。

### 模拟特定请求

```bash
# 带自定义 Header
curl -H "Authorization: Bearer xxx" -H "Content-Type: application/json" \
  -d '{"orderNo":"20260525001"}' https://api.example.com/api/pay

# 指定 DNS 解析（绕过本地 DNS）
curl --resolve api.example.com:443:10.0.1.10 https://api.example.com/health

# 指定网卡（多网卡环境）
curl --interface eth1 https://api.example.com/health

# 限制连接时间
curl --connect-timeout 3 --max-time 10 https://api.example.com/health
```

## 工具二：tcpdump — 抓包之王

tcpdump 是 Linux 上最强大的抓包工具。很多网络问题只有看了抓包才能确认根因。

### 常用抓包命令

```bash
# 抓取某个端口的流量
tcpdump -i eth0 port 8080 -nn -c 100

# 抓取特定主机之间的流量
tcpdump -i eth0 host 10.0.1.10 and port 8080 -nn

# 抓取 TCP SYN 包（排查连接建立问题）
tcpdump -i eth0 'tcp[tcpflags] & (tcp-syn) != 0' -nn

# 抓取 TCP RST 包（排查连接被重置问题）
tcpdump -i eth0 'tcp[tcpflags] & (tcp-rst) != 0' -nn

# 抓取 HTTP 请求（明文 HTTP）
tcpdump -i eth0 port 80 -A -s 0 | grep -E "^(GET|POST|PUT|DELETE)"

# 保存到文件，用 Wireshark 分析
tcpdump -i eth0 port 8080 -w /tmp/capture.pcap -c 1000
```

### 实战：排查连接超时

```bash
# 监控到目标服务器的 TCP 连接状态
tcpdump -i eth0 host 10.0.1.10 and port 8080 -nn -tttt

# 观察结果：
# 正常情况：
# 10:00:00.001 SYN →
# 10:00:00.002 SYN-ACK ←
# 10:00:00.002 ACK →
# 10:00:00.005 HTTP GET →
# 10:00:00.010 HTTP 200 ←

# 异常情况（目标端口不开放）：
# 10:00:00.001 SYN →
# 10:00:00.001 RST ←    ← 立即收到 RST，说明端口没开

# 异常情况（目标不可达）：
# 10:00:00.001 SYN →
# 10:00:03.001 SYN →     ← 3 秒后重试
# 10:00:09.001 SYN →     ← 再次重试
```

## 工具三：Wireshark — 图形化抓包分析

tcpdump 负责在服务器上抓包，Wireshark 负责在本地电脑上分析。配合使用效果最好。

```bash
# 在服务器上抓包保存
tcpdump -i eth0 port 443 -w /tmp/https_capture.pcap -c 10000

# 下载到本地
scp user@server:/tmp/https_capture.pcap ./

# 用 Wireshark 打开分析
```

Wireshark 中常用的过滤器：

```
# 过滤 HTTP 请求
http.request.method == "POST"

# 过滤 TCP 重置包
tcp.flags.reset == 1

# 过滤慢响应（响应时间 > 1 秒）
http.time > 1

# 过滤 TLS 握手失败
tls.alert_message

# 过滤特定 IP
ip.addr == 10.0.1.10
```

**Wireshark 的 TCP 流追踪**是排查问题的杀手锏：右键任意一个包 → Follow → TCP Stream，可以看到完整的请求-响应对话。

## 工具四：Arthas — Java 应用网络诊断

Arthas 是阿里巴巴开源的 Java 诊断工具，可以在线排查应用层面的网络问题。

### 追踪网络调用耗时

```bash
# 追踪 RestTemplate 的请求耗时
watch org.apache.http.impl.execchain.HttpClientExecute \
  proceed '{params[0].getURI(), returnObj.getStatusLine(), #cost}' -n 20 -x 2

# 追踪 DNS 解析
watch java.net.InetAddress getAllByName0 \
  '{params[0], returnObj}' -n 20

# 追踪 Socket 连接
watch java.net.Socket connect \
  '{params[0], throwExp}' -n 20
```

### 查看网络相关线程状态

```bash
# 查看所有 BLOCKED 的线程（可能卡在网络操作上）
thread -b

# 查看所有线程状态
thread -n 5

# 查看 IO 相关的线程
thread --state WAITING | grep -i "http\|socket\|nio"
```

### 实时监控方法耗时

```bash
# 监控某个服务方法的 RT 分布
monitor -c 5 com.example.service.PaymentService pay

# 输出：
# timestamp  class            method  total  success  fail  avg-rt(ms)  fail-rate
# 2026-05-25 PaymentService   pay     100    98       2     45.3        2%
```

## 真实案例一：偶发接口超时（DNS 问题）

**现象**：用户服务调用地址服务，P99 从 10ms 偶尔飙到 500ms。

**排查过程**：

```bash
# 1. 用 arthas 追踪接口耗时
monitor -c 10 com.example.service.AddressService resolve

# 发现：大部分请求 5ms，偶尔 300~500ms

# 2. 用 arthas 追踪 Socket 连接
watch java.net.Socket connect '{params, #cost}' -n 50

# 发现：慢请求的 Socket.connect 耗时 300ms+

# 3. 怀疑是 DNS 问题，用 tcpdump 验证
tcpdump -i eth0 port 53 -nn -tttt

# 发现：某些 DNS 查询确实要 300ms+，且都是对内网域名的查询

# 4. 检查 Java DNS 缓存配置
# 发现 -Dsun.net.inetaddr.ttl 没有设置（默认 -1，永久缓存）
# 意味着 DNS 结果一旦过期，重新查询就可能触发慢解析
```

**解决**：设置 JVM 参数 `-Dsun.net.inetaddr.ttl=30`，同时优化内网 DNS 服务器配置。

## 真实案例二：连接被重置（RST）

**现象**：调用第三方支付接口，偶发 `Connection reset by peer`。

**排查过程**：

```bash
# 1. tcpdump 抓 RST 包
tcpdump -i eth0 host pay-api.third-party.com and 'tcp[tcpflags] & (tcp-rst) != 0' -nn

# 发现：RST 来自本机，不是远端

# 2. 检查连接池配置
# 发现 OkHttp 连接池空闲超时 5 分钟，但第三方 keepalive_timeout 是 30 秒
# 第三方在 30 秒后关闭连接，OkHttp 不知道，继续使用已关闭的连接

# 3. Wireshark 分析确认
# 用 tcpdump 抓包后用 Wireshark 打开
# 确认：本机在连接被服务端关闭 40 秒后发了一个请求，服务端返回 RST
```

**解决**：将 OkHttp 的空闲连接回收时间设为 25 秒（小于第三方的 30 秒），同时开启 `retryOnConnectionFailure(true)`。

## 真实案例三：部分用户 HTTPS 握手失败

**现象**：部分用户反馈网站打不开，大部分用户正常。

**排查过程**：

```bash
# 1. 用不同方式测试
curl -v https://www.example.com
# 正常

curl -v --tls-max 1.2 https://www.example.com
# 正常

openssl s_client -connect www.example.com:443 -tls1
# TLS 1.0 握手失败（符合预期，我们禁用了 TLS 1.0）

# 2. 让报错的用户提供 User-Agent
# 发现是 Android 4.4 的 WebView，只支持 TLS 1.0

# 3. 但同一台手机的 Chrome 可以正常访问
# 原因：Chrome 内置了 TLS 1.2 支持，但 WebView 用的是系统库

# 4. 检查 Nginx 证书链配置
openssl s_client -connect www.example.com:443 -showcerts
# 发现中间证书缺失，部分旧客户端无法自动下载中间证书
```

**解决**：修复证书链（使用 `fullchain.pem` 而不是 `domain.crt`），同时评估是否支持 TLS 1.0（安全原因，最终决定不支持，引导用户升级浏览器）。

## 排查流程总结

遇到线上网络问题，按照这个流程来：

```
1. 确认问题范围
   ├── 是所有用户还是部分用户？
   ├── 是所有接口还是特定接口？
   └── 是持续的还是偶发的？

2. 快速诊断
   ├── curl -v 测试目标接口
   ├── curl -w 测量各阶段耗时
   └── 确定卡在哪个阶段（DNS / TCP / TLS / HTTP）

3. 深入排查
   ├── DNS 问题 → dig + 检查 /etc/resolv.conf
   ├── TCP 问题 → tcpdump + Wireshark
   ├── TLS 问题 → openssl s_client + Wireshark
   └── 应用层问题 → arthas + 日志

4. 根因确认
   └── 能复现的问题才是能解决的问题
```

## 工具速查表

| 场景 | 首选工具 | 命令 |
|------|----------|------|
| 测试接口是否可达 | curl | `curl -v URL` |
| 测量请求各阶段耗时 | curl | `curl -w '...' -o /dev/null -s URL` |
| 排查连接建立问题 | tcpdump | `tcpdump -i eth0 host X port Y` |
| 排查连接重置问题 | tcpdump | `tcpdump 'tcp[tcpflags] & (tcp-rst) != 0'` |
| 分析 TLS 握手问题 | openssl | `openssl s_client -connect host:port` |
| Java 应用网络追踪 | arthas | `watch` / `trace` / `monitor` |
| 抓包后详细分析 | Wireshark | 打开 .pcap 文件 |
| 检查端口是否监听 | ss/netstat | `ss -tlnp \| grep 8080` |
| 检查路由是否可达 | traceroute | `traceroute target-ip` |

最后一句话：**网络排查的核心能力不是记住工具命令，而是理解网络协议的分层模型**。知道问题大概在哪个层，然后用对应的工具去验证，这就是排查思路。
