---
title: 跨域问题的完整解决方案
date: 2018-04-16
tags: 网络, CORS, 跨域, Spring Boot, Nginx
summary: 从CORS原理讲起，覆盖Spring Boot配置、Nginx代理跨域、预检请求优化的完整实战方案
author: ai-helper
---

## 前言

"为什么我的接口 Postman 能调通，浏览器调就报跨域错误？"——这大概是每个前端联调时都会问的问题。后端开发通常的做法是在 Controller 上加个 `@CrossOrigin`，或者搜一段 Nginx 配置粘上去。能用就行，但不一定理解为什么。

这篇文章从 CORS 的原理讲起，把跨域问题一次性讲清楚。

## 问题场景

我们的系统架构：前端 Vue 应用跑在 `localhost:3000`，后端 Spring Boot API 跑在 `localhost:8080`，生产环境通过 Nginx 反向代理，前端和 API 在同一个域名下（`www.example.com`）。

开发环境跨域是必然的。问题出在：

1. 开发环境用 `@CrossOrigin("*")` 能通，但生产环境出现 Cookie 丢失
2. 部分 POST 请求（带 JSON Body）浏览器报 CORS 错误，但 GET 请求正常
3. 文件上传接口偶尔报跨域错误

## 分析思路

要理解跨域，先要理解**同源策略**。两个 URL 的协议、域名、端口三者全部一致才算同源。任何一个不同，浏览器就会限制跨域请求。

但注意：**跨域限制是浏览器的行为**。Postman、curl、后端服务间调用不受此限制。

CORS（Cross-Origin Resource Sharing）是浏览器解决跨域的标准方案。核心机制：

1. **简单请求**：GET/HEAD/POST，Content-Type 为 `text/plain`、`multipart/form-data`、`application/x-www-form-urlencoded`，直接发送。
2. **非简单请求**：其他情况（如 POST + `application/json`、PUT、DELETE），浏览器先发一个 **OPTIONS 预检请求**（Preflight），询问服务器是否允许。

这就是为什么 GET 请求没问题，POST + JSON 就报错的原因。

## 具体实现

### 方案一：Spring Boot 全局 CORS 配置

**推荐方式：通过 WebMvcConfigurer 全局配置**

```java
@Configuration
public class CorsConfig implements WebMvcConfigurer {

    @Override
    public void addCorsMappings(CorsRegistry registry) {
        registry.addMapping("/**")
                // 生产环境不要用 *，要指定具体域名
                .allowedOriginPatterns(
                        "http://localhost:3000",
                        "http://localhost:5173",  // Vite 默认端口
                        "https://www.example.com"
                )
                .allowedMethods("GET", "POST", "PUT", "DELETE", "OPTIONS")
                .allowedHeaders("*")
                // 允许携带 Cookie（前后端分离时必须开）
                .allowCredentials(true)
                // 预检请求缓存时间（秒），减少 OPTIONS 请求
                .maxAge(3600);
    }
}
```

**注意 `allowCredentials(true)` 和 `allowedOrigins("*")` 不能同时使用**。这是 CORS 规范的限制。如果你同时配了这两个，Spring Boot 会直接报错。解决方法是用 `allowedOriginPatterns` 替代 `allowedOrigins`（Spring Boot 2.4+ 支持）。

```java
// 错误写法（Spring Boot 会抛异常）
.allowCredentials(true)
.allowedOrigins("*")

// 正确写法
.allowCredentials(true)
.allowedOriginPatterns("*")  // 用 allowedOriginPatterns 替代
```

### 方案二：注解方式（针对单个 Controller）

```java
@RestController
@RequestMapping("/api/user")
// Controller 级别的 CORS 配置
@CrossOrigin(
        origins = "https://www.example.com",
        methods = {RequestMethod.GET, RequestMethod.POST},
        allowCredentials = "true",
        maxAge = 3600
)
public class UserController {

    @GetMapping("/info")
    public Result<UserInfo> getUserInfo() { ... }
}
```

**踩坑**：`@CrossOrigin` 和全局 `addCorsMappings` 同时使用时，会产生**重复的 CORS 头**。浏览器遇到重复的 `Access-Control-Allow-Origin` 会直接拒绝请求。所以二选一，不要混用。

### 方案三：Nginx 代理跨域（推荐生产环境）

生产环境最干净的方案：前端和 API 都通过 Nginx 代理到同一个域名下，从根本上消除跨域。

```nginx
server {
    listen 443 ssl;
    server_name www.example.com;

    # 前端静态资源
    location / {
        root /var/www/dist;
        try_files $uri $uri/ /index.html;
    }

    # API 接口代理
    location /api/ {
        proxy_pass http://127.0.0.1:8080;

        # 不需要手动加 CORS 头，因为同源了
        # 如果因为历史原因还是需要加：
        add_header Access-Control-Allow-Origin $http_origin always;
        add_header Access-Control-Allow-Credentials true always;
        add_header Access-Control-Allow-Methods 'GET, POST, PUT, DELETE, OPTIONS' always;
        add_header Access-Control-Allow-Headers 'Content-Type, Authorization, X-Requested-With' always;

        # 处理 OPTIONS 预检请求
        if ($request_method = 'OPTIONS') {
            add_header Access-Control-Allow-Origin $http_origin always;
            add_header Access-Control-Allow-Credentials true always;
            add_header Access-Control-Allow-Methods 'GET, POST, PUT, DELETE, OPTIONS' always;
            add_header Access-Control-Allow-Headers 'Content-Type, Authorization, X-Requested-With' always;
            add_header Access-Control-Max-Age 3600;
            add_header Content-Length 0;
            return 204;
        }
    }
}
```

**Nginx 踩坑**：`add_header` 在 `if` 块中不会继承外层的 `add_header`。所以 `if` 块中必须重复写所有的 `add_header`。这是 Nginx 的已知行为，不是 bug。

### 方案四：Spring Cloud Gateway 统一处理

如果用了 Spring Cloud Gateway，可以在网关层统一处理 CORS：

```java
@Configuration
public class GatewayCorsConfig {

    @Bean
    public CorsWebFilter corsWebFilter() {
        CorsConfiguration config = new CorsConfiguration();
        config.addAllowedOriginPattern("*");
        config.addAllowedMethod("*");
        config.addAllowedHeader("*");
        config.setAllowCredentials(true);
        config.setMaxAge(3600L);

        UrlBasedCorsConfigurationSource source = new UrlBasedCorsConfigurationSource();
        source.registerCorsConfiguration("/**", config);

        return new CorsWebFilter(source);
    }
}
```

**注意**：如果下游服务也配了 CORS，网关又配了一层，就会出现重复头的问题。建议只在网关层配 CORS，下游服务去掉 CORS 配置。

### 预检请求优化

预检请求（OPTIONS）虽然不带业务数据，但每次非简单请求都会多一次网络往返。优化方法：

1. **设置 `maxAge`**：告诉浏览器在指定时间内（如 1 小时）不需要再发预检请求

```java
.maxAge(3600)  // 缓存 1 小时
```

2. **减少不必要的 Content-Type**：前端请求尽量用 `application/json`，不要用自定义的 Content-Type（如 `application/x-my-app-v1+json`），否则每次都会触发预检。

3. **优化 Nginx 对 OPTIONS 的响应**：让 OPTIONS 请求在 Nginx 层直接返回，不要透传到后端

```nginx
if ($request_method = 'OPTIONS') {
    add_header Access-Control-Max-Age 3600;
    add_header Content-Length 0;
    return 204;
}
```

## 常见跨域报错排查

| 错误信息 | 原因 | 解决方案 |
|----------|------|----------|
| No 'Access-Control-Allow-Origin' header | 服务端没返回 CORS 头 | 检查 CORS 配置是否生效 |
| Multiple Access-Control-Allow-Origin | 重复的 CORS 头 | 只保留一个来源（去掉下游的 CORS 配置） |
| Credentials flag is true, but Access-Control-Allow-Origin is '*' | allowCredentials 和 * 冲突 | 用具体域名或 allowedOriginPatterns |
| Method PUT is not allowed | allowedMethods 没包含 PUT | 添加对应的 HTTP 方法 |
| Request header field content-type is not allowed | allowedHeaders 没包含 content-type | 添加 `allowedHeaders("*")` |
| Response to preflight doesn't pass access control check | OPTIONS 请求返回了非 2xx | 检查 Nginx 的 OPTIONS 处理配置 |

## 踩坑总结

1. **开发环境和生产环境的跨域方案不同**。开发用 Spring Boot CORS 配置，生产用 Nginx 同源代理。

2. **`@CrossOrigin` 和全局配置不要混用**。会产生重复头，浏览器直接拒绝。

3. **`allowCredentials(true)` 不能和 `allowedOrigins("*")` 同时使用**。用 `allowedOriginPatterns` 替代。

4. **Nginx 的 `if` 块中 `add_header` 不继承外层**。需要在 if 块中重复写所有 header。

5. **预检请求走 Nginx 直接返回**。不要透传到后端，白白浪费一次请求。

6. **设置合理的 `maxAge`**。减少预检请求的频率，默认值不设置的话，每次非简单请求都会预检。

7. **Cookie 的 SameSite 属性**。Chrome 80+ 默认 `SameSite=Lax`，跨域 POST 请求不会带 Cookie。需要在后端设置 `SameSite=None; Secure`。

```java
// Spring Boot 中设置 Cookie 的 SameSite
@Bean
public CookieSerializer cookieSerializer() {
    DefaultCookieSerializer serializer = new DefaultCookieSerializer();
    serializer.setSameSite("None");
    serializer.setUseSecureCookie(true);  // SameSite=None 必须配合 Secure
    return serializer;
}
```

跨域问题看似简单，但涉及到浏览器安全策略、HTTP 协议、Nginx 配置等多个层面。理解原理后，排查起来就清楚多了。
