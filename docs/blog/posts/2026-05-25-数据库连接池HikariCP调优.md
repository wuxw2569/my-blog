---
title: 数据库连接池HikariCP调优
date: 2026-05-25
tags: 数据, HikariCP, 连接池, 性能调优
summary: 从HikariCP参数详解到连接泄漏排查，分享中小厂在数据库连接池调优中的实战经验，包括与Druid的对比选型。
author: ai-helper
---

## 问题场景

线上服务隔三差五报 `HikariPool-1 - Connection is not available, request timed out after 30000ms`。一开始以为是并发量大，加了 `maximumPoolSize` 从20调到50，结果更惨——MySQL 报 `Too many connections`，直接把数据库搞挂了。

连接池这个东西，配置不对就是定时炸弹。

## 分析思路

### 1. 先搞清楚连接池的工作原理

HikariCP 的核心机制很简单：维护一个固定大小的连接池，请求来了取连接、用完还回去。关键在于：

- `maximumPoolSize` 不是越大越好，它受 MySQL `max_connections` 限制
- 连接池大小的合理公式：**connections = (core_count * 2) + effective_spindle_count**
- 对于8核SSD服务器，`(8 * 2) + 1 = 17`，所以20个连接已经够了

**那为什么会不够用？** 因为连接被"借"走后没还回来——连接泄漏。

### 2. HikariCP 核心参数详解

我们的生产配置：

```yaml
spring:
  datasource:
    hikari:
      # 最小空闲连接数（默认等于maximumPoolSize）
      minimum-idle: 10
      # 最大连接数
      maximum-pool-size: 20
      # 连接超时时间（从池中获取连接的最大等待时间）
      connection-timeout: 30000
      # 空闲连接最大存活时间（超过此时间的空闲连接会被回收）
      idle-timeout: 600000
      # 连接最大存活时间（强制回收，防止MySQL端超时断开）
      max-lifetime: 1800000
      # 连接泄漏检测阈值（借出超过此时间未归还的连接会被记录日志）
      leak-detection-threshold: 60000
      # 连接测试查询
      connection-test-query: SELECT 1
      # 连接校验超时
      validation-timeout: 5000
```

**每个参数的含义和踩坑点**：

**maximum-pool-size（最大连接数）**

```java
// 错误认知：越大越好
// 实际上：每个连接占MySQL的一个线程 + 内存
// 20个应用实例 * 50连接 = 1000连接，MySQL默认max_connections=151
// 不调整MySQL配置，应用层加再多连接也没用
```

MySQL 端也要配合调整：

```sql
-- 查看当前最大连接数
SHOW VARIABLES LIKE 'max_connections';
-- 调整为500（根据实际服务器配置）
SET GLOBAL max_connections = 500;
```

**max-lifetime（最大存活时间）**

这个值必须比 MySQL 的 `wait_timeout` 小，否则MySQL端已经断开了连接，应用还在用，就会报 `Communications link failure`。

```sql
SHOW VARIABLES LIKE 'wait_timeout';  -- 默认28800秒（8小时）
```

所以我们设 `max-lifetime=1800000`（30分钟），远小于8小时。

**leak-detection-threshold（泄漏检测阈值）**

这个参数是排查连接泄漏的神器。设为60秒后，如果一个连接被借出超过60秒还没归还，HikariCP 会打印一条告警日志并附带借出时的堆栈：

```
WARNING: Connection leak detection triggered for com.mysql.cj.jdbc.ConnectionImpl@1a2b3c4d, stack trace follows
java.lang.Exception: Apparent connection leak detected
    at com.zaxxer.hikari.HikariDataSource.getConnection(HikariDataSource.java:128)
    at com.example.service.OrderService.queryOrder(OrderService.java:45)
    ...
```

看到这个堆栈就知道是哪个方法借了连接没还。

### 3. 连接泄漏的排查

**常见泄漏原因**：

**原因一：手动获取连接没关闭**

```java
// 错误写法
public void badMethod() {
    Connection conn = dataSource.getConnection();
    PreparedStatement ps = conn.prepareStatement("SELECT ...");
    ResultSet rs = ps.executeQuery();
    // 如果中间抛异常，conn永远不会归还到连接池
    processResult(rs);
    conn.close();
}

// 正确写法：try-with-resources
public void goodMethod() {
    try (Connection conn = dataSource.getConnection();
         PreparedStatement ps = conn.prepareStatement("SELECT ...");
         ResultSet rs = ps.executeQuery()) {
        processResult(rs);
    }
}
```

**原因二：事务未正确关闭**

```java
// 错误写法：编程式事务异常时未回滚
public void badTransaction() {
    TransactionStatus status = txManager.getTransaction(new DefaultTransactionDefinition());
    try {
        doSomething();
        // 如果这里抛异常，事务没回滚，连接也没释放
        txManager.commit(status);
    } catch (Exception e) {
        // 忘记回滚
        log.error("操作失败", e);
    }
}

// 正确写法
public void goodTransaction() {
    TransactionStatus status = txManager.getTransaction(new DefaultTransactionDefinition());
    try {
        doSomething();
        txManager.commit(status);
    } catch (Exception e) {
        txManager.rollback(status);
        throw e;
    }
}
```

**原因三：MyBatis 嵌套查询的连接持有**

```java
// 危险：在遍历结果时发起新的数据库查询（N+1问题也会导致连接长时间占用）
@Select("SELECT * FROM t_order WHERE user_id = #{userId}")
List<Order> selectByUserId(Long userId);

// OrderMapper 中
@Select("SELECT * FROM t_order_detail WHERE order_id = #{orderId}")
List<OrderDetail> selectDetails(Long orderId);
```

如果在 Service 层这样用：

```java
List<Order> orders = orderMapper.selectByUserId(userId);
for (Order order : orders) {
    // 每次循环都获取新连接执行查询，外层连接还在持有
    order.setDetails(orderMapper.selectDetails(order.getOrderId()));
}
```

外层查询的连接一直不释放，循环越多连接占用越久。优化方案：用 `@One` 注解做 JOIN 查询，或者用 IN 查询一次性取详情。

### 4. 连接池监控

我们用 Spring Boot Actuator + Prometheus + Grafana 做连接池监控：

```yaml
# application.yml
management:
  endpoints:
    web:
      exposure:
        include: health,metrics,prometheus
  metrics:
    tags:
      application: order-service
```

关键监控指标：

```java
@Component
public class HikariCPMetrics {

    @Autowired
    private HikariDataSource dataSource;

    @Scheduled(fixedRate = 30000)
    public void reportMetrics() {
        HikariPoolMXBean poolMXBean = dataSource.getHikariPoolMXBean();
        if (poolMXBean != null) {
            log.info("【连接池状态】活跃连接={}, 空闲连接={}, 等待连接={}, 总连接={}",
                poolMXBean.getActiveConnections(),
                poolMXBean.getIdleConnections(),
                poolMXBean.getThreadsAwaitingConnection(),
                poolMXBean.getTotalConnections());
        }
    }
}
```

告警规则（Prometheus）：

```yaml
# 等待连接数持续大于0，说明连接池不够用
- alert: HikariCPConnectionWait
  expr: hikaricp_connections_pending > 0
  for: 2m
  labels:
    severity: warning
  annotations:
    summary: "连接池等待数大于0"

# 活跃连接数持续接近最大连接数
- alert: HikariCPConnectionExhaustion
  expr: hikaricp_connections_active / hikaricp_connections_max > 0.8
  for: 5m
  labels:
    severity: critical
```

## 与 Druid 的对比

我们之前用的 Druid，后来换成了 HikariCP。对比一下：

| 维度 | HikariCP | Druid |
|------|----------|-------|
| 性能 | 更快（字节码优化，无锁设计） | 略慢 |
| 监控 | 依赖外部（Actuator） | 自带强大监控页面 |
| SQL防火墙 | 不支持 | 支持 |
| 内置统计 | 基础 | 详细（SQL执行时间、慢SQL统计） |
| Spring Boot默认 | 是（2.x起默认） | 否 |
| 维护状态 | 活跃 | 活跃（阿里系） |

**我们的选择**：

- 内部管理系统用 Druid（方便看慢SQL、内置监控页面开发测试阶段很好用）
- 高并发线上服务用 HikariCP（性能更好，监控交给 Prometheus + Grafana）

如果你不需要 Druid 的SQL防火墙和内置监控，选 HikariCP 就对了。Spring Boot 2.x 之后默认就是 HikariCP。

## 踩坑总结

1. **连接池大小不是越大越好**。`(CPU核数 * 2) + 磁盘数` 是合理的起点，然后根据监控数据微调。超过30个连接基本不会有收益。
2. **leak-detection-threshold 一定要开**。线上默认设60秒，开发环境可以设10秒。这个参数几乎零开销，但能在第一时间发现连接泄漏。
3. **max-lifetime 必须小于 MySQL 的 wait_timeout**。否则会出现"连接已被服务端关闭"的间歇性错误。
4. **不要在长事务中持有连接**。查询完及时释放，不要在循环里嵌套查询。
5. **监控比调参更重要**。连接池的问题往往不在连接池本身，而是慢SQL、锁等待导致连接被长时间占用。先解决慢SQL，再调连接池。
6. **多数据源场景下每个数据源单独配连接池**。总连接数 = 各数据源连接数之和，要算好别超MySQL的 `max_connections`。
