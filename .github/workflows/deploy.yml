name: Deploy VitePress site to GitHub Pages

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      # ✅ 安装 pnpm（一定要放在 setup-node 前面）
      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8.6.0
          run_install: false  # 不在这里自动执行 install

      # ✅ 安装 Node.js，并启用 pnpm 缓存
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'

      # ✅ 安装依赖
      - name: Install dependencies
        run: pnpm install

      # ✅ 构建 VitePress
      - name: Update AI daily report
        run: python fetch_daily_report.py

      - name: Build VitePress site
        run: pnpm run build

      # ✅ 防止 Jekyll 误处理 .vitepress 文件夹
      - name: Disable Jekyll
        run: touch docs/.vitepress/dist/.nojekyll

      # ✅ 部署到 GitHub Pages（自动推送到 gh-pages 分支）
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: docs/.vitepress/dist
